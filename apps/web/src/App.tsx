import "./styles.css";

export function App() {
  return (
    <main className="app-shell">
      <section className="status-panel" aria-labelledby="status-title">
        <p className="eyebrow">Adaptive Core Network</p>
        <h1 id="status-title">ACN Control Surface</h1>
        <p className="summary">
          Initial monorepo scaffold for versioned neural training workflows.
        </p>
      </section>
    </main>
  );
}

